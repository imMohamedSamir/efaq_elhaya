import 'package:flutter/material.dart';

class LocationMsgWidget extends StatelessWidget {
  const LocationMsgWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
